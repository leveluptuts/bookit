// /** @type {import('@sveltejs/kit').Handle} */
// export async function handle({ event, resolve }) {
// 	const response = await resolve(event, {
// 		// if env isn't testing and is not admin page
// 		ssr: false
// 	});

// 	return response;
// }
